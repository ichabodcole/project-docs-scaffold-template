#!/usr/bin/env bun
/**
 * docs-lint — the gate for the documentation layer.
 *
 * Placeholder. Phase 0 of docs/projects/okf-frontmatter-layer/plan.md wires the
 * toolchain (Bun, `npm run check`, the pre-commit hook, CI) before any rule
 * exists, so every later phase lands against a gate that is already green and
 * already running. Phase 1 gives it the ported link/anchor pass; Phase 2 gives
 * it the two frontmatter tiers.
 */

console.log("docs-lint: no checks yet");
process.exit(0);
