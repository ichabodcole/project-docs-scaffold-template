── library (graph tier) ────────────────────────────────
OK — links, anchors, OKF frontmatter, catalog reachability and `related` edges all valid across 4 files.

── workbench (thin tier) ───────────────────────────────
OK — no problems.
0 tracked page(s) outside docs/, links only  (`lint.exclude` takes one out)

docs-lint: clean
